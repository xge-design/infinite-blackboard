/**
 * 无限黑板 — 常量定义
 *
 * 所有可调参数集中于此。
 */

/* ═══════════════════════════════════════════════════
   统一句式
   ═══════════════════════════════════════════════════ */

/** 唯一的内容格式——整部作品永远只有这一种句式 */
export const PROMPT_PREFIX = '在我死去之前，我想';
export const PROMPT_SUFFIX = '。';
export const PROMPT_PLACEHOLDER = '写下你的愿望……';

/* ═══════════════════════════════════════════════════
   渲染层级阈值
   ═══════════════════════════════════════════════════ */

export const ZOOM_LEVELS = {
  /** 低于此值：仅渲染点阵 */
  DOT_MAX: 0.25,
  /** 高于此值：文字开始显现 */
  TEXT_MIN: 0.25,
  /** 高于此值：渲染完整文本 */
  FULL_TEXT_MIN: 0.6,
} as const;

/* ═══════════════════════════════════════════════════
   画布视觉常量（世界空间单位）
   ═══════════════════════════════════════════════════ */

export const CANVAS = {
  /** 消息卡片宽度（世界像素） */
  CARD_WIDTH: 550,
  /** 消息卡片高度（世界像素） */
  CARD_HEIGHT: 250,
  /** 网格单元间距 */
  GRID_GAP: 120,
  /** 远景点半径（世界像素） */
  DOT_RADIUS: 10,
  /** 屏幕上最小点半半径（像素） */
  MIN_SCREEN_DOT: 4,
  /** 屏幕上最大点半半径（像素） */
  MAX_SCREEN_DOT: 16,
  /** 背景色（十六进制数字，供 PixiJS 使用） */
  BG_COLOR_HEX: 0x2a2a2a,
  /** 背景色（CSS 字符串） */
  BG_COLOR: '#2a2a2a',
} as const;

/* ═══════════════════════════════════════════════════
   交互参数
   ═══════════════════════════════════════════════════ */

export const INTERACTION = {
  /** 最小缩放 */
  MIN_ZOOM: 0.03,
  /** 最大缩放 */
  MAX_ZOOM: 4.0,
  /** 滚轮灵敏度 */
  ZOOM_SPEED: 0.0012,
  /** 惯性摩擦系数（0–1，越小越快停止） */
  MOMENTUM_FRICTION: 0.92,
  /** 触发惯性的最小速度 */
  MOMENTUM_THRESHOLD: 0.5,
} as const;

/* ═══════════════════════════════════════════════════
   动画时间
   ═══════════════════════════════════════════════════ */

export const ANIMATION = {
  /** 弹窗进出时长（毫秒） */
  MODAL_DURATION: 300,
} as const;
